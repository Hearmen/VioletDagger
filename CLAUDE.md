# VioletDagger 项目规范

- 所有文档均写入 `docs/` 目录中。
- 任何操作都必须基于文档——先有文档依据，再动手实现或修改。
- 对文档的所有修改都必须经过用户 review，未经确认不得实际写入文件。


## 设计规范
- 设计文档必须细致，每个模块都应该有独立的设计文档，模块之间的交互必须有对应的接口文档
- 设计文档初始化必须使用 sub-agent 来对文档进行检查，后续根据用户需求进行检查

## 编码规范

- 所有的编码必须基于设计文档，先写详细设计文档，然后再编码
- 任务对于代码的修改必须先修改对应的文档，经用户 review 后才可以进行实际的写入
